{"timestamp":"1678917163","reviewRef":"refs/pull/632/head","targetRef":"refs/heads/main","requester":"GnomedDev","description":"Rewrite Heap headers in C++\n\nThis removes the ugly reinterpret casts that were being used due to C++ using the C headers, and simultaneously makes some C headers unused, so removes them.\r\n\r\nI chose to rewrite `HeapCollection` as simply having each heap pointer as a field, instead of an array, as having an array + enum seemed more clunky.","baseCommit":"4fbf45d6424222325cfed06de5daaf00bb9e3db4"}
