{"timestamp":"1685132489","reviewRef":"refs/pull/763/head","targetRef":"refs/heads/main","requester":"GnomedDev","description":"Decompile ResourceManager::loadCourse\n\nThis is split off from #655, it loosly decompiles loadCourse while avoiding an extra buffer (writing directly to jobContext-\u003efilename instead of maintaining a buffer then copying) and asserts the file has loaded correctly, which prevents worse crashes if the filePath is wrong or something else is going wrong in course loading.\r\n\r\nThis PR also removes headers and symbols for `preloadCourseAsync` as it doesn't actually do anything with CourseCache stubbed.","baseCommit":"3c134e54c4c1d869190c923faf8ee76046c2852e"}
